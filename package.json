{
  "name": "mojireact-typing-game",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check:public-info": "node scripts/check-public-info.js",
    "check:syntax": "node --check app/script.js && node --check app/romaji-engine.js",
    "test": "npm run check:syntax && npm run check:public-info"
  }
}
